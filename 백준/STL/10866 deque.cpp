#include <iostream>
#include <list>
#include <pair>
#include <string>

using namespace std;

int main() {
	cout << "hello world";
	/*
	list<pair<int, int>> a;
	int n;
	cin >> n;
	cout << n;
	/*
	for (int i = 1;i < n+1;i++) {
		int j;
		cin >> j;
		a.push_back({ i,j });
	}
	
	auto it = a.begin();
	while (n--) {
		cout << it->first << ' ';
		int tmp = it->second;
		
		//a.erase(it);
	}*/
}